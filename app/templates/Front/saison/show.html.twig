{% extends 'base_front.html.twig' %}

{% block title %}Saison{% endblock %}

{% block body %}
    <h1>Saison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saison.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ saison.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('saison_index') }}">back to list</a>

    <a href="{{ path('saison_edit', {'id': saison.id}) }}">edit</a>

    {{ include('Front/saison/_delete_form.html.twig') }}
{% endblock %}
