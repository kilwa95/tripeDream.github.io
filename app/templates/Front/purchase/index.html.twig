{% extends 'base_front.html.twig' %}

{% block title %}purchase index{% endblock %}

{% block body %}
<div class="d-flex align-items-center" style="height: 137px; background: #1c1d1f; color: white;">
    <div style="font-size:30px;font-weight: 400;padding-left:195px;"> Mes achats</div>
</div>

<div class="container mt-5 mb-5">
 <div class="row">
        <div class="col-9 mr-3">
            <div class="d-flex flex-column">
            {% if  purchases is defined %}
            {% for purchase in purchases %}
                <div style=" height:90px;" class="d-flex flex-row justify-content-between  card">
                    <div class="d-flex flex-row" style="max-width:320px; min-width:320px; ">
                        <img src="{{ vich_uploader_asset(purchase) }}" alt="{{ purchase.name }}" />
                        <div style="color:#1c1d1f;font-weight:700;" class="pl-1">{{purchase.name}}</div>

                    </div>
                    <div class="d-flex flex-column pt-2">
                    {# <a style="color:#a435f0;" href="{{path('purchase_delete',{'id': purchase.id})}}">Supprimer</a> #}
                    <a style="color:#a435f0;" href="{{path('voyage_show',{'id': purchase.id})}}">Fiche produit</a>
                    </div>
                    <div class="pr-2 pt-2"style="color:#a435f0; ">{{purchase.tarif|first.prix}} €</div>
                </div>
            {% endfor %}
            {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}

