<div class="mt-3 mb-5 w-50 card">
    <div class="card-header">
        ajouter un commentaire
    </div>
    <div class="card-body">
        {{ form_start(form) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.description) }}
        <div class="d-flex ">
            <div><i class=" star fa fa-star mr-2 mt-2"></i></div>
           <div> {{ form_row(form.compteur) }}</div>
        </div>

    </div>
    <div class="card-footer">
        <button class="btn btn-info">{{ button_label|default('Publier') }}</button>
    </div>
    {{ form_end(form) }}

</div>