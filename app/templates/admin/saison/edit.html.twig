{% extends 'admin/index.html.twig' %}

{% block title %}Modifier une saison{% endblock %}

{% block contentpage %}

<div class="container-fluid">
    <div class="fade-in">
        <div class="row">
            <div class="col">
                <div class="card">
                    <div class="card-header text-center"><h3><strong>Modifier une saison</strong></h3></div>
                    {{ include('admin/saison/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>

{% set entity = 'saison' %}
{% include 'admin/back_to_list_card.html.twig' %}

{% endblock %}
