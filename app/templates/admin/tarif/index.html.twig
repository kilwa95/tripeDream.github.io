{% extends 'admin/index.html.twig' %}

{% block title %}Liste des tarifs{% endblock %}

{% block datatabscripts %}
    {% include 'admin/datatable.html.twig' %}
{% endblock %}

{% block contentpage %}
<input id="entity" name="entity" type="hidden" value="tarif">
<input id="url" name="url" type="hidden" value="fetch-tarifs">

<div class="container-fluid">
    <div class="fade-in">
        <div class="row">
            <div class="col">
                <div class="card card-accent-success">
                    <div class="card-header"><h4><strong>Liste des tarifs</strong></h4>
                        <div class="card-header-actions">
                            <a type="button" class="btn btn-outline-primary" href="{{ path('admin_tarif_new') }}">
                                <i class="bi bi-person-plus"></i>
                            </a>
                        </div>
                    </div>
                    {% for message in app.flashes('success') %}
                        <div class="alert alert-success alert-dismissible fade show">
                            {{ message }}
                            <button class="close" type="button" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">×</span></button>
                        </div>
                    {% endfor %}

                    {% for message in app.flashes('danger') %}
                        <div class="alert alert-danger alert-dismissible fade show">
                            {{ message }}
                            <button class="close" type="button" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">×</span></button>
                        </div>
                    {% endfor %}
                    <div class="collapse show" id="coll">
                        <div class="card-body">
                            <table id="table_data" data-toggle="bootgrid" class="table table-responsive-sm table-bordered">
                                <thead>
                                    <tr>
                                        <th data-column-id="id" data-type="numeric">ID</th>
                                        <th data-column-id="prix">Prix</th>
                                        <th data-column-id="depart">Date de départ</th>
                                        <th data-column-id="retour">Date de retour</th>
                                        <th data-column-id="capacite">Capacité</th>
                                        <th data-column-id="1" data-formatter="voyageId" data-sortable="false">ID voyage</th>
                                        <th data-column-id="commands" data-formatter="commands" data-sortable="false">Opérations</th>
                                    </tr>
                                </thead>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

